source 'http://rubygems.org'

gem 'json'

# Use the GitHub repo version - it is compatible with Ruby 1.9.2 where the RubyGems version is not :(
gem 'xmpp4r-simple', :git => 'git://github.com/blaine/xmpp4r-simple.git'

